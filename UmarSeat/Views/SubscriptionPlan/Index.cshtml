@model IEnumerable<UmarSeat.Models.subscriptionPlan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameSubscriptionPlan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.duration)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subscriptionDurationType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subscriptionPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameSubscriptionPlan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.duration)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subscriptionDurationType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subscriptionPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_SubscriptionPlan }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_SubscriptionPlan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_SubscriptionPlan })
        </td>
    </tr>
}

</table>
